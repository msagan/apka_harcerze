%h3
  Lista planów rocznych
= link_to 'Dodaj plan', new_year_plan_path, class: 'btn btn-primary'

%br
%br
%table#cycles_table{cellpadding: "0", cellspacing:"0", border: "0", class: "datatable table table-striped table-bordered"}
  %thead
    %tr
      %th Tytuł
      %th Gromada
      %th Sprawności do zdobycia
      %th Akcje
  %tbody
    -@teams.each do |team|
      -team.year_plans.each do |year_plan|
        %tr{id: "j-#{year_plan.id}"}
          %td=year_plan.title
          %td=year_plan.team.name
          %td=year_plan.badges.pluck(:name)
          %td
            = link_to 'Pokaż cyckle', year_plan_path(year_plan), class: 'btn-sm btn-primary'
            = link_to 'Edytuj', edit_year_plan_path(year_plan), class: 'btn-sm btn-primary'
            = link_to 'Usuń', year_plan_path(year_plan), method: :delete, confirm: 'Na pewno? (Tej operacji nie można cofnąć!)', class: 'btn-sm btn-danger'
