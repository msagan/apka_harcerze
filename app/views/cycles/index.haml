%h3
  Lista cykli

= link_to 'Dodaj cykl', new_cycle_path(@year_plan), class: 'btn btn-primary'

%br
%br
%table#cycles_table{cellpadding: "0", cellspacing:"0", border: "0", class: "datatable table table-striped table-bordered"}
  %thead
    %tr
      %th Nazwa
      %th Gromada
      %th Data początku
      %th Data końca
      %th Akcje
  %tbody
    -@teams.each do |team|
      -team.cycles.each do |cycle|
        %tr{id: "j-#{cycle.id}"}
          %td=cycle.name
          %td=cycle.team.name
          %td=cycle.start_date.to_date
          %td=cycle.stop_date.to_date
          %td
            = link_to 'Pokaż spotkania', cycle_path(cycle), class: 'btn-sm btn-primary'
            = link_to 'Edytuj', edit_cycle_path(cycle), class: 'btn-sm btn-primary'
            = button_to 'Usuń', cycle_path(cycle), method: :delete, data: {confirm: 'Na pewno? (Tej operacji nie można cofnąć!)'}, class: 'btn-sm btn-danger'
